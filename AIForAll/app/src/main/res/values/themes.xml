<resources>
    <!-- Minimal framework-only theme (no appcompat/material XML dependency
         needed) — Compose owns all real theming via presentation/theme/. -->
    <style name="Theme.AIForAll" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
    </style>
</resources>
